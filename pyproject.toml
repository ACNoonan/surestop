[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "certified-pruner"
version = "0.1.0"
description = "Early-kill training runs with a conformal bound on killing runs that would have ended well"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Adam Noonan" }]
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "scipy>=1.10"]
keywords = ["hyperparameter-optimization", "early-stopping", "pruning", "conformal", "optuna"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[project.optional-dependencies]
optuna = ["optuna>=4"]
test = ["pytest", "optuna>=4"]

[project.urls]
Homepage = "https://github.com/ACNoonan/certified-pruner"
Issues = "https://github.com/ACNoonan/certified-pruner/issues"

[tool.setuptools.packages.find]
where = ["src"]
include = ["certified_pruner*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
